{
  "routeSchema": {
    "title": "Departures by route",
    "type": "object",
    "properties": {
      "route": {
        "type": "string",
        "title": "Route",
        "enum": [],
        "enumNames": []
      },
      "direction": {
        "type": "string",
        "title": "Direction",
        "enum": [],
        "enumNames": []
      },
      "stop": {
        "type": "string",
        "title": "Stop",
        "enum": [],
        "enumNames": []
      }
    }
  },
  "routeUiSchema": {
    "route": {
      "ui:title": "Route",
      "ui:widget": "select",
      "ui:placeholder": "Select a route"
    },
    "direction": {
      "ui:title": "Direction",
      "ui:widget": "select",
      "ui:placeholder": "Select a direction"
    },
    "stop": {
      "ui:title": "Stop",
      "ui:widget": "select",
      "ui:placeholder": "Select a stop"
    }
  },
  "stopSchema": {
    "title": "Departures by stop",
    "type": "object",
    "properties": {
      "stop": {
        "type": "string",
        "pattern": "^\\d*$",
        "title": "Stop"
      }
    }
  },
  "stopUiSchema": {
    "stop": {
      "ui:title": "Stop",
      "ui:widget": "search",
      "ui:placeholder": "Enter a stop number"
    }
  }
}
